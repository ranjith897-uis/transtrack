{
  "name": "transtrack-gps-simulator",
  "version": "1.0.0",
  "description": "Simulates hardware GPS trackers driving vehicles along route paths, posting to the backend's /tracking/ingest endpoint. Stand-in until real hardware is procured.",
  "main": "dist/simulate.js",
  "type": "commonjs",
  "scripts": {
    "dev": "tsx watch src/simulate.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/simulate.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "tsx": "^4.16.0",
    "typescript": "^5.5.0"
  }
}
